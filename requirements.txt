numpy>=1.24

# --- OpenSearch backend (BACKEND=opensearch) ---
opensearch-py>=2.4.0
requests>=2.28
orjson>=3.9.0
# Optional: only needed when OS_USE_AWS_AUTH=true (SigV4 to managed OpenSearch)
requests-aws4auth>=1.2.3
boto3>=1.28

# --- PostgreSQL / pgvector backend (BACKEND=pgvector) ---
psycopg2-binary>=2.9
pgvector>=0.2.5
